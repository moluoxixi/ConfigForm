import { setupAntdVue, StatusTabs } from '@moluoxixi/ui-antd-vue'

/**
 * setup：变量或常量声明。
 * 所属模块：`playground/vue/src/ui/antd-vue.ts`。
 * 该声明用于描述模块的对外契约或内部结构边界。
 */
export const setup = setupAntdVue
export { StatusTabs }
