import { setupElementPlus, StatusTabs } from '@moluoxixi/ui-element-plus'

/**
 * setup：定义该模块复用的常量配置。
 * 所属模块：`playground/vue/src/ui/element-plus.ts`。
 * 该声明用于描述模块的对外契约或内部结构边界。
 */
export const setup = setupElementPlus
export { StatusTabs }
