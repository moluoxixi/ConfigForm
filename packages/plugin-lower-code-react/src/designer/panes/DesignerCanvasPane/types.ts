import type { ComponentProps } from 'react'
import type { CanvasPanel } from '../../canvas/CanvasPanel'

/**
 * Designer Canvas Body Renderer Props：描述该模块使用的类型别名语义。
 * 所属模块：`packages/plugin-lower-code-react/src/designer/panes/DesignerCanvasPane/types.ts`。
 * 该声明用于描述模块的对外契约或内部结构边界。
 */
export type DesignerCanvasBodyRendererProps = ComponentProps<typeof CanvasPanel>
