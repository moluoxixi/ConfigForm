{
  "name": "config-form",
  "type": "module",
  "private": true,
  "scripts": {
    "dev:ep": "pnpm -C playgrounds/element-plus-playground dev",
    "dev:antd": "pnpm -C playgrounds/antd-vue-playground dev",
    "build": "pnpm -r --filter \"./packages/*\" build",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "pnpm -r --filter \"./packages/*\" test",
    "test:coverage": "pnpm -C packages/ConfigForm test:coverage",
    "typecheck": "pnpm -r --filter \"./packages/*\" typecheck"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:dev",
    "@types/node": "catalog:type",
    "eslint": "catalog:dev",
    "eslint-plugin-format": "catalog:dev",
    "jiti": "catalog:dev",
    "typescript": "catalog:dev",
    "vitest": "catalog:dev"
  }
}
